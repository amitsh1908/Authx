# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/adminpanel

# JWT Secret (use a strong random string in production)
JWT_SECRET=your-secret-key-change-in-production

# CORS - Frontend URL (comma-separated for multiple origins)
CORS_ORIGIN=http://localhost:5173

# ===== PRODUCTION NOTES =====
# 1. Use a strong, random JWT_SECRET (min 32 characters)
# 2. Set NODE_ENV=production
# 3. Configure CORS_ORIGIN to your actual frontend domain
# 4. Use MongoDB Atlas or secured MongoDB instance
# 5. Never commit .env file to version control
